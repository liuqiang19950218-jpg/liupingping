# 架构实施状态

更新日期：2026-08-21

## 已启动

- 已完成现有代码、数据存储与 k3s 部署方式审计。
- 已建立 PostgreSQL、Redis、MinIO 的 k3s 基础清单。
- 已建立 PostgreSQL 每日备份与 30 天保留策略。
- 已建立首版 PostgreSQL 业务数据模型，覆盖季度、客户、账套、发票、差额、跟进、资料、导入历史、文件和审计。
- 数据模型明确保留“空值”和“0”的区别；未填写发票号的差额不进入发票核验。
- 已增加基础设施一键应用脚本与只读验收脚本；真实密钥清单明确排除 Git。
- 已增加只允许连接 `192.168.51.182:8000` 的生产部署入口，避免误用旧的 94 服务器脚本。
- 已增加 k3s Secret 自动初始化脚本；生产密码只保存在集群 Secret，不写入 Git 或部署配置。

## 当前现状

- `192.168.51.182:8000` 已运行本次生产版本，k3s Deployment 为 `1/1`，Pod 状态为 `Running`。
- 当前生产镜像为 `quarterly-recon:prod-20260821-101423`，首页及共享状态接口健康检查通过。
- 当前生产仍是单应用 Pod + Wrangler 本地持久目录；PostgreSQL、Redis、MinIO 已准备基础清单，但尚未切换为应用正式数据源。
- 当前浏览器 localStorage/IndexedDB 与服务器 JSON 快照仍是主要业务数据来源。
- 182 服务器已保存本机同步的 2026 Q2 快照；旧 Sites 地址中的 2026 Q1 仍保存在原浏览器站点存储中，需要通过一次性迁移入口导出并按季度合并。
- 部署前 Wrangler 数据备份位于 `/home/liupp/apps/quarterly-recon/backups/wrangler-20260821-100111.tgz`。
- 生产数据库切换尚未执行，现有数据未删除、未覆盖。
- Git 提交已保存在本地；GitHub 推送仍需在本机完成账号授权。

## 下一批实施

1. 在旧 Sites 地址访问 `?migration=export` 下载迁移包，再在 182 地址访问 `?migration=import` 合并导入；服务端保留已有季度，只补入缺失季度。
2. 核验服务器快照中的 Q1/Q2 季度数量、客户数量、金额、空值、发票核验和跟进状态，并生成迁移清单。
3. 在 182 服务器创建数据服务并验证备份恢复。
4. 增加服务端鉴权、业务 API、乐观锁和审计日志。
5. 将核验后的现有数据导入 PostgreSQL，再切换 PostgreSQL/Redis/MinIO 为正式数据源。
6. 数据迁移和并发验证完成后，将应用扩展到两个副本。

## 发布规则

- 不设独立测试地址，用户直接在生产地址验收。
- 仍必须通过最小技术门禁：构建成功、数据库迁移成功、容器健康、生产页面可访问、备份可生成。
- 每次发布使用不可变版本标签，失败时回滚上一版本；数据库只使用向前兼容迁移。
